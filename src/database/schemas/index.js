const userSchema = require('./userSchema');
const sessionSchema = require('./sessionSchema');
const boardSchema = require('./boardSchema');
const resetPasswordRequestsSchema = require('./resetPasswordRequests');

module.exports = {
  userSchema,
  sessionSchema,
  boardSchema,
  resetPasswordRequestsSchema,
};
